---
applications:
- name: start.steeltoe.io
  buildpacks:
   - dotnet_core_buildpack
  memory: 512M
  command: cd ${HOME} && ./InitializrApi
  env:
    ASPNETCORE_ENVIRONMENT: Production
  services:
   
