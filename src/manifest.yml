---
applications:
- name: startsteeltoe
  buildpacks:
   - dotnet_core_buildpack
  memory: 512M
  command: cd ${HOME} && ./Steeltoe.Initializr
  env:
    ASPNETCORE_ENVIRONMENT: Production
  services:
   
